# Prometheus configuration for dark
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "dark"
    static_configs:
      - targets: ["dark:9090"]
    metrics_path: /metrics
    scrape_interval: 10s
