# Prometheus configuration for arkd-rs
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "arkd"
    static_configs:
      - targets: ["arkd:9090"]
    metrics_path: /metrics
    scrape_interval: 10s
