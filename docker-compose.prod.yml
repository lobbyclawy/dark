version: '3.8'

services:
  dark:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "7070:7070"
      - "7071:7071"
    volumes:
      - dark-data:/home/dark/.dark
      - ./config.toml:/home/dark/.dark/config.toml:ro
    environment:
      - RUST_LOG=info
      - DARK_WALLET_PASSPHRASE=${DARK_WALLET_PASSPHRASE:-}
    restart: unless-stopped
    depends_on:
      - bitcoin

  bitcoin:
    image: ruimarinho/bitcoin-core:latest
    command: >
      -regtest
      -rpcallowip=0.0.0.0/0
      -rpcbind=0.0.0.0
      -rpcuser=admin1
      -rpcpassword=123
      -server
    ports:
      - "18443:18443"

volumes:
  dark-data:
