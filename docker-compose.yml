version: "3.8"

# dark Development Environment
# Usage: docker-compose up -d

services:
  # =============================================================================
  # Bitcoin Core (regtest)
  # =============================================================================
  bitcoin:
    image: ruimarinho/bitcoin-core:24
    container_name: dark-bitcoin
    command:
      - -regtest
      - -server
      - -rpcuser=dark
      - -rpcpassword=dark
      - -rpcallowip=0.0.0.0/0
      - -rpcbind=0.0.0.0
      - -txindex
      - -fallbackfee=0.00001
      - -printtoconsole
    ports:
      - "18443:18443"  # RPC
      - "18444:18444"  # P2P
    volumes:
      - bitcoin_data:/home/bitcoin/.bitcoin
    healthcheck:
      test: ["CMD", "bitcoin-cli", "-regtest", "-rpcuser=dark", "-rpcpassword=dark", "getblockchaininfo"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dark

  # =============================================================================
  # PostgreSQL Database
  # =============================================================================
  postgres:
    image: postgres:16-alpine
    container_name: dark-postgres
    environment:
      POSTGRES_USER: dark
      POSTGRES_PASSWORD: dark
      POSTGRES_DB: dark
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dark"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dark

  # =============================================================================
  # Redis Cache
  # =============================================================================
  redis:
    image: redis:7-alpine
    container_name: dark-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dark

  # =============================================================================
  # Esplora (optional block explorer)
  # =============================================================================
  esplora:
    image: blockstream/esplora
    container_name: dark-esplora
    depends_on:
      bitcoin:
        condition: service_healthy
    environment:
      NETWORK: regtest
      DAEMON_URL: http://dark:dark@bitcoin:18443
    ports:
      - "3000:80"    # HTTP API
      - "8094:8094"  # Electrum
    networks:
      - dark
    profiles:
      - esplora  # Only start with --profile esplora

  # =============================================================================
  # dark Server (when built)
  # =============================================================================
  dark:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dark-server
    depends_on:
      bitcoin:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DARK_CONFIG: /etc/dark/config.toml
      RUST_LOG: info
    ports:
      - "50051:50051"  # gRPC
      - "8080:8080"    # REST
    volumes:
      - ./config.toml:/etc/dark/config.toml:ro
      - dark_data:/data
    networks:
      - dark
    profiles:
      - full  # Only start with --profile full

volumes:
  bitcoin_data:
  postgres_data:
  redis_data:
  dark_data:

networks:
  dark:
    driver: bridge
